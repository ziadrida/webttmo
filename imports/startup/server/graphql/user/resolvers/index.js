// User - change words User and user
// user graphql
// import resolvers for type, query and mutations
import User from './user';
import Query from './query';
// const Mutation = require('./mutation');

const resolvers = {
  User,
  Query,
  // Mutation,
};

export default resolvers;
